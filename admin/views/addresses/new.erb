<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:addresses, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:addresses, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :address, url(:addresses, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'addresses/form', :locals => { :f => f } %>
 <% end %>
</div>
