<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:firm_infos, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:firm_infos, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:firm_infos, :edit, :id => @firm_info.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :firm_info, url(:firm_infos, :update, :id => @firm_info.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'firm_infos/form', :locals => { :f => f } %>
  <% end %>
</div>
