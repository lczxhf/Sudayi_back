<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:states, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:states, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :state, url(:states, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'states/form', :locals => { :f => f } %>
 <% end %>
</div>
