<% error = @city.errors.include?(:province_id) %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :province_id, :class => 'control-label' %>
  <div class='controls'>
    <%= f.select(:province_id, :collection => Province.all, :fields => [:name, :id], :class => 'form-control input-xlarge input-with-feedback', :autofocus => true) %>
    <span class='help-inline'><%= error ? f.error_message_on(:province_id) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @city.errors.include?(:name) %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :name, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :name, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:name) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @city.errors.include?(:code) %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :code, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :code, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:code) : "Ex: a simple text" %></span>
  </div>
</fieldset>


<div class="form-actions">
  <%= f.submit pat(:save), :class => 'btn btn-primary' %>
  &nbsp;
  <%= f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue' %>
  &nbsp;
  <%= link_to pat(:cancel), url(:cities, :index), :class => 'btn btn-default' %>
</div>