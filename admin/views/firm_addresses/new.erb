<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:firm_addresses, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:firm_addresses, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :firm_address, url(:firm_addresses, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'firm_addresses/form', :locals => { :f => f } %>
 <% end %>
</div>
