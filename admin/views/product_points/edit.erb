<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:product_points, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:product_points, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:product_points, :edit, :id => @product_point.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :product_point, url(:product_points, :update, :id => @product_point.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'product_points/form', :locals => { :f => f } %>
  <% end %>
</div>
