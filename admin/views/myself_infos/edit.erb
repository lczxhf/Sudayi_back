<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:myself_infos, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:myself_infos, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:myself_infos, :edit, :id => @myself_info.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :myself_info, url(:myself_infos, :update, :id => @myself_info.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'myself_infos/form', :locals => { :f => f } %>
  <% end %>
</div>
