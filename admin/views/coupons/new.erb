<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:coupons, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:coupons, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :coupon, url(:coupons, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'coupons/form', :locals => { :f => f } %>
 <% end %>
</div>
