<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:order_settings, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:order_settings, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:order_settings, :edit, :id => @order_setting.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :order_setting, url(:order_settings, :update, :id => @order_setting.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'order_settings/form', :locals => { :f => f } %>
  <% end %>
</div>
